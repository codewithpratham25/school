import pandas as pd
import sqlalchemy
import pymysql

#main code 
class storeDetail():
    def storeDetail(owner,store,storeCat):
        owner = owner
        store = store
        storeCat = storeCat
        print(f'Owner Name ---> {owner}     Store Name ---> {store}')
        print(f'Store Category ---> {storeCat}')

    
